import Vue from 'vue';
import Vuex from 'vuex';

import collaborator from './collaborator';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    collaborator,
  },
});
